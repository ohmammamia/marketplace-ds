# Project 3 — Capacity and Resource Allocation Optimisation for Service Delivery

Start with `docs/problem_definition.md`, then `docs/methodology.md`; every non-obvious
choice is in `docs/decision_log.md`, what does not work yet in `docs/limitations.md`.

Run: `python -m project3_allocation.src.pipeline configs/project3.yaml` from the repository root.
Outputs land in `outputs/project3/`. Notebooks in `notebooks/` are narrative only.
