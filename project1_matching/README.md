# Project 1 — Intelligent Matching and Ranking for Service Allocation

Start with `docs/problem_definition.md`, then `docs/methodology.md`; every non-obvious
choice is in `docs/decision_log.md`, what does not work yet in `docs/limitations.md`.

Run: `python -m project1_matching.src.pipeline configs/project1.yaml` from the repository root.
Outputs land in `outputs/project1/`. Notebooks in `notebooks/` are narrative only.
