# Project 2 — From Unstructured Service Feedback to Actionable Intelligence

Start with `docs/problem_definition.md`, then `docs/methodology.md`; every non-obvious
choice is in `docs/decision_log.md`, what does not work yet in `docs/limitations.md`.

Run: `python -m project2_feedback_nlp.src.pipeline configs/project2.yaml` from the repository root.
Outputs land in `outputs/project2/`. Notebooks in `notebooks/` are narrative only.
