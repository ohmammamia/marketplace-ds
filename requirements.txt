# Pinned exactly. Committed outputs/ and data/synthetic/ are reproducible only
# against these versions: ranking metrics, solver tie-breaks and the pickled
# Project 1 model all shift across minor releases of pandas / scikit-learn.
# Refresh by bumping here, re-running `make all`, and committing the new artefacts.
pandas==3.0.5
numpy==2.4.6
scipy==1.17.1
scikit-learn==1.9.1
pulp==3.3.2
PyYAML==6.0.1
joblib==1.6.0
matplotlib==3.11.2
pytest==9.1.1
nbformat==5.11.1
nbconvert==7.17.1
ipykernel==7.3.0
